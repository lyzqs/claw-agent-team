apiVersion: 1

providers:
  - name: agent-team-grafana-local-observability
    orgId: 1
    folder: Agent Team / Grafana
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/agent-team-grafana
